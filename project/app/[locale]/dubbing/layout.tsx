export async function generateMetadata() {
  return {
    title: 'Dubbing Studio | Onyx Studios',
    description:
      'Global AI dubbing with human quality control: multilingual translation, lip-sync, voice preservation, and final mix delivery.',
  };
}

export default function DubbingLayout({ children }: { children: React.ReactNode }) {
  return children;
}
